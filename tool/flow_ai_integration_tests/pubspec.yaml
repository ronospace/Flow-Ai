name: flow_ai_integration_tests
description: Flow AI device integration test suite.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flow_ai:
    path: ../..

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
